export enum NestedMaterialType {
  ITEM = 'ITEM',
  CATALOG = 'CATALOG',
}

export enum PopoverListItemActionEnum {
  RENAME = 'RENAME',
  COPY = 'COPY',
  DELETE = 'DELETE',
  SHARE = 'SHARE',
  RUN = 'RUN',
  ADD_FOLDER = 'ADD_FOLDER',
  ADD_MATERIAL = 'ADD_MATERIAL',
}

export enum DropTypeEnum {
  BEFORE = 'before',
  AFTER = 'after',
  INNER = 'inner',
  NONE = 'none',
}
